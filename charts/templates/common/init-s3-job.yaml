apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "app.fullname" . }}-init-s3
  labels:
    {{- include "app.labels.custom" (dict "context" . "component" "init-s3") | nindent 4 }}
    app.kubernetes.io/component: init-s3
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
    {{- with include "app.annotations.custom" (dict "context" . "component" "init-s3") }}
    {{- . | nindent 4 }}
    {{- end }}
spec:
  backoffLimit: 5
  template:
    metadata:
      labels:
        app: {{ include "app.name" . }}-init-s3
        {{- include "app.labels.custom" (dict "context" . "component" "init-s3") | nindent 8 }}
      {{- with include "app.annotations.custom" (dict "context" . "component" "init-s3") }}
      annotations:
        {{- . | nindent 8 }}
      {{- end }}
    spec:
      restartPolicy: OnFailure
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: check-seaweedfs-s3-ready
          image: {{ .Values.image.registry }}/{{ .Values.s3Init.initContainer.image.repository }}:{{ default (default .Chart.AppVersion .Values.image.tag) .Values.s3Init.initContainer.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - 'sh'
            - '-c'
            - |
              set -eu

              until nc -vz {{ include "app.fullname" . }}-seaweedfs-filer {{ .Values.seaweedfs.s3.port }}; do
                echo "Waiting for SeaweedFS S3 service";
                sleep 1;
              done;
          {{- if .Values.s3Init.initContainer.resources }}
          resources: {{- toYaml .Values.s3Init.initContainer.resources | nindent 12 }}
          {{- end }}
      containers:
        - name: init-s3
          image: {{ .Values.image.registry }}/{{ .Values.s3Init.image.repository }}:{{ default (default .Chart.AppVersion .Values.image.tag) .Values.s3Init.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - python
            - -m
            - common.scripts.init_s3
          envFrom:
            - configMapRef:
                name: {{ include "app.fullname" . }}-settings
            {{- with include "app.externalSecrets.envFrom" . }}{{ . | nindent 12 }}{{- end }}
          {{- if .Values.s3Init.resources }}
          resources: {{- toYaml .Values.s3Init.resources | nindent 12 }}
          {{- end }}
