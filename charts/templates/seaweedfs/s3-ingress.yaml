{{- if .Values.seaweedfs.s3.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "app.fullname" . }}-seaweedfs-s3
  labels:
    {{- include "app.labels.custom" (dict "context" . "component" "seaweedfs") | nindent 4 }}
    app.kubernetes.io/component: seaweedfs-s3
  annotations:
    {{- toYaml .Values.seaweedfs.s3.ingress.annotations | nindent 4 }}
    {{- if .Values.certificate.requireClientCert }}
    traefik.ingress.kubernetes.io/router.tls.options: {{ .Release.Namespace }}-{{ include "app.fullname" . }}-mtls@kubernetescrd
    {{- end }}
spec:
  ingressClassName: {{ .Values.seaweedfs.s3.ingress.className }}
  rules:
    - host: s3.{{ .Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "app.fullname" . }}-seaweedfs-filer
                port:
                  number: {{ .Values.seaweedfs.s3.port }}
  {{- if .Values.seaweedfs.s3.ingress.tls.enabled }}
  tls:
    - secretName: {{ include "app.fullname" . }}-certificate
      hosts:
        - s3.{{ .Values.domain }}
  {{- end }}
{{- end }}
