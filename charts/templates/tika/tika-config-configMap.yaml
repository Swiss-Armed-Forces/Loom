apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-tika-config
  labels:
    {{- include "app.labels.custom" (dict "context" . "component" "tika") | nindent 4 }}
    app.kubernetes.io/component: tika
  {{- with include "app.annotations.custom" (dict "context" . "component" "tika") }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
data:
  tika-config.xml: |
    {{- tpl (.Files.Get "files/tika/tika-config.xml.tpl") (dict "taskTimeoutMillis" (mul .Values.tika.taskTimeoutSeconds 1000)) | nindent 4 }}
