fluentd:
  enabled: false

prometheus:
  collectClusterMetrics: false

elasticsearch:
  replicaCount: 3
  shardsCount: 3
  shardsReplicasCount: 1
  config:
    cluster:
      routing:
        allocation:
          disk:
            threshold_enabled: true
