fluentd:
  enabled: false

prometheus:
  collectClusterMetrics: false

elasticsearch:
  replicaCount: 3
  shardCount: 3
  config:
    cluster:
      routing:
        allocation:
          disk:
            threshold_enabled: true
