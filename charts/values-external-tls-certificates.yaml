# External TLS Certificates
#
# Use this when your cluster manages TLS certificates centrally via Vault
# and you do not want Loom to provision its own ClusterIssuer.
#
clusterIssuer:
  enabled: false

certificate:
  enabled: true
  issuerRef:
    name: external-vault-issuer
