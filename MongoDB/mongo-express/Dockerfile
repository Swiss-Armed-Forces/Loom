# Image version
ARG MONGODB_EXPRESS_IMAGE_VERSION="1.0.2-20-alpine3.19"

ARG CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX
FROM ${CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX}/mongo-express:${MONGODB_EXPRESS_IMAGE_VERSION}